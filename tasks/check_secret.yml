platform: linux
image_resource:
  type: registry-image
  source:
    repository: alpine
params:
  SECRET_VALUE: ((ips.ips_db_password))
run:
  path: /bin/sh
  args:
    - -ce
    - echo "${SECRET_VALUE}"

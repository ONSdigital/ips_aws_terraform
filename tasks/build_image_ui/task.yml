platform: linux
image_resource:
  type: registry-image
  source:
    repository: vito/oci-build-task
inputs:
  - name: ips-ui-git
    path: .
  - name: ips-repo
outputs:
  - name: image
run:
  path: build
